---
id: h-2026-04-30T19-02-04-7greens-01
projectid: 7greens
phase: 01
task_id: null
created_at: 2026-04-30T19:02:04.490Z
created_by: unknown
claimed_by: null
claimed_at: null
completed_at: null
priority: normal
estimated_context: mechanical
runtime_preference: codex-cli
---
# Task: 7greens site polish (Codex A)

You are Codex executing on the 7greens project. Your CWD is `sites/7greens/` (the submodule root). The site is **live in production** at `https://7greens-six.vercel.app`. Don't break it.

## Read these first
- `app/page.tsx` (composes home sections)
- `components/sections/Hero.tsx` (needs motion)
- `components/sections/FounderStory.tsx` (image is currently a gradient card — looks blurry / generic, replace with a real photo)
- `lib/vcs/DevPanel.tsx` (current local VCS panel — needs quick-prompts upgrade)
- `lib/vcs/cid.ts`
- `package.json` (current deps)
- `public/photos/brand/field-to-family-01.jpeg` and `field-to-family-02.png` (real brand photos available)
- `public/photos/produce/web-photos-*.{jpeg,png}` (25 produce photos)

## What to do

### 1) Fix the founder story image (HIGH priority)

`components/sections/FounderStory.tsx` currently builds a fake "Uncle Paul" portrait card from a gradient + text. Operator says it's blurry / unsatisfying. Replace with a real photo composition:

- Use `public/photos/brand/field-to-family-01.jpeg` and `field-to-family-02.png` as the visual anchor
- Or use 2-3 produce photos arranged as an editorial collage
- Keep the asymmetric "stacked photo cards with rotation" feel from the hero, but executed with REAL imagery
- Add a small floating eyebrow chip ("Founded 2019" or "Tyler, TX") layered on top
- The pull-quote and narrative text stay; only the visual block on the left needs replacement

### 2) Add background motion to the Hero (HIGH priority)

`components/sections/Hero.tsx` has static blur orbs. Operator wants actual movement. Add ONE of:

- A slow-drifting animated gradient mesh behind the headline (CSS `@keyframes` on the existing blur orbs — translate them slowly in opposite directions, ~20s loop, `prefers-reduced-motion` aware)
- A subtle Ken Burns effect on the photo stack (slow scale + translate)
- Floating particle SVG (small organic shapes drifting up — leaves / seeds appropriate to the brand)

Pick the one that fits the Heritage Modern aesthetic best — earthy, restrained, NOT a tech-brand sparkle effect. Honor `@media (prefers-reduced-motion: reduce)` and disable.

### 3) Upgrade VCS to use the gad-visual-context npm package — IF it works

`gad-visual-context@0.1.0` was just published to npm. Try integrating:

```sh
pnpm add gad-visual-context
```

(or `npm install` if pnpm complains).

The package has a known peer dep on `@portfolio/ui` which is NOT published. So:
- If installing pulls @portfolio/ui successfully (via workspace link in monorepo): use the package's `<DevPanel>`, `<DevIdProvider>`, and `<Identified>` components from `gad-visual-context`. Replace the local `lib/vcs/DevPanel.tsx`. Mount `<DevIdProvider>` inside layout's body and a `<DevPanel>` somewhere.
- If installing fails because @portfolio/ui can't resolve: STAY on the local `lib/vcs/` and just enhance it (#4 below) — don't waste time fighting the peer dep. Note in your final report: "package install requires @portfolio/ui peer; staying on local VCS until that's also published."

### 4) Add quick prompts to the VCS panel

Whether using `gad-visual-context` package OR enhanced local `lib/vcs/DevPanel.tsx`, add a "quick prompts" feature: each cid item gets 3 small action chips that copy a prefab prompt to clipboard:

- `Edit` — copies "Modify the {cid} section to: [your change]"
- `Ask` — copies "Explain how the {cid} section works and where it's defined."
- `Move` — copies "Move/restructure the {cid} section so that: [your goal]"

(The `{cid}` is the actual cid string, substituted in.)

When the user clicks a chip, copy the substituted prompt to clipboard and flash a tiny "copied" toast/badge. Keyboard shortcut to cycle prompts on the focused row would be nice but optional.

Also: make the panel slightly bigger (380-420px wide) and add a search box at the top that filters cids by substring.

### 5) Verify + ship

- `pnpm install` (if you added gad-visual-context — race-safe now, no parallel agent touching lockfile)
- `pnpm --filter 7greens build` — if it succeeds locally, great. If `pnpm` not in path, use `npm run build` from sites/7greens.
- `git add -A && git commit -m "<conventional message>"` per change cluster (split into 2-3 commits if it makes sense: founder image, hero motion, vcs quick prompts)
- `git push origin main`
- `vercel deploy --prod --yes` from sites/7greens to redeploy production
- Verify the production URL works after deploy

### 6) Final report (under 250 words)

- What you did for each of #1-#5
- gad-visual-context outcome (used / fell-back to local + why)
- Commit SHAs + push status
- Production deployment URL after redeploy
- Any test failures or blockers left behind

## Constraints

- DO NOT touch any file outside `sites/7greens/`.
- DO NOT modify `.planning/` files (that's framework state).
- DO NOT skip git hooks.
- DO NOT touch the parent monorepo's lockfile.
- Do not introduce framer-motion or any motion library — pure CSS animations only.
- If the build fails locally and you can't fix it in 2-3 attempts, REVERT your change and surface the issue in the final report.
- Be opinionated about the founder image composition — operator wants it to look good, so commit to a strong visual treatment.

Operator is at limited session budget (~5% with 1.5hr to go). Move fast. Quality > completeness. Ship what works.