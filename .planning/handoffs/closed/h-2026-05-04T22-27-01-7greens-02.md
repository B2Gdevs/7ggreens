---
id: h-2026-05-04T22-27-01-7greens-02
projectid: 7greens
phase: 02
task_id: null
created_at: 2026-05-04T22:27:01.958Z
created_by: unknown
claimed_by: team-g-1
claimed_at: 2026-05-04T23:11:29.825Z
completed_at: 2026-05-04T23:14:29.705Z
priority: normal
estimated_context: bounded
risk: safe
time: deep
surface: api-bound
---
Phase 02 follow-up — bootstrap 7greens media intake from operator's pre-scraped Google Sheet.

OPERATOR-SUPPLIED DATA:
- Sheet URL: https://docs.google.com/spreadsheets/d/1rec6Nuk-rz5uYwsVss2RivcY8kAvPkI7tRhGEuQZBxQ/edit?gid=0#gid=0
- Columns: Date, From, Subject, Link (where Link is a drive.google.com URL)
- Source: scraped via Apps Script (provided below) from gmail subject:'Item shared with you'

ORIGINAL APPS SCRIPT (preserved as the canonical Drive-link extractor — port to TypeScript for the integration adapter):
```javascript
function exportSharedLinks() {
  const query = 'subject:"Item shared with you"';
  const maxThreads = 500;
  const sheet = SpreadsheetApp.getActive().getActiveSheet();
  sheet.clearContents();
  sheet.getRange(1, 1, 1, 4).setValues([['Date', 'From', 'Subject', 'Link']]);
  const threads = GmailApp.search(query, 0, maxThreads);
  let row = 2;
  threads.forEach(thread => {
    const messages = thread.getMessages();
    messages.forEach(msg => {
      const body = msg.getBody();
      const plain = msg.getPlainBody();
      let link = extractDriveLink(body) || extractDriveLink(plain) || extractFirstHttpLink(body) || extractFirstHttpLink(plain);
      if (link) {
        sheet.getRange(row, 1, 1, 4).setValues([[msg.getDate(), msg.getFrom(), msg.getSubject(), link]]);
        row++;
      }
    });
  });
}
function extractDriveLink(text) { if (!text) return null; const re = /https:\/\/drive\.google\.com\/[^\s"'<>()]+/i; const m = text.match(re); return m ? m[0] : null; }
function extractFirstHttpLink(text) { if (!text) return null; const re = /https:\/\/[^\s"'<>()]+/i; const m = text.match(re); return m ? m[0] : null; }
```

THIS HANDOFF SCOPE (bounded, ship-now):
1. Save the Apps Script verbatim to sites/7greens/scripts/legacy-gmail-scrape.gs as historical reference + onboarding doc.
2. Create sites/7greens/scripts/import-from-sheet.mjs that: takes the sheet URL, fetches as CSV (use the gviz CSV endpoint or operator-provided CSV export), parses rows, prints {date, from, subject, drive_link} entries.
3. Create sites/7greens/scripts/fetch-drive-asset.mjs that: takes a drive.google.com share link, follows redirect to direct-download URL, saves bytes locally to a staging dir.
4. NO tagging/labeling/moderation in this handoff — that's phase 120's full scope. This handoff is the data ingestion bridge only.
5. Document the import flow in sites/7greens/docs/media-intake.md with operator-readable step-by-step.

DO-NOT in this handoff: do NOT add payloadcms (operator standing rule); do NOT call Vision APIs (phase 120); do NOT touch Supabase Storage upload yet (defer to phase 120 once tagging schema lands).

Output: 3 scripts + 1 doc, committed. Per SOUL.md: SITREP-format report, close with gaps.