# 7greens schema diff — variant=project

**Agent:** claude-schema-cli

Schema diff: variant=project, path=C:/Users/benja/Documents/custom_portfolio/sites/7greens, match=100% (6/6 required). Missing optional (4): .planning/gad-config.toml, .planning/notes/, .planning/handoffs/, .planning/sessions/. Extras (12): app/, components/, gad-config.toml (root-level), lib/, next.config.mjs, package.json, postcss.config.mjs, public/, README.md, scripts/, tsconfig.json, vercel.json — all standard Next.js site artifacts, recommend adding an optional 'site' supplement to the project schema.
