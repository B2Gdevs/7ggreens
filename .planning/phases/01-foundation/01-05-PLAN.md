---
phase: "01"
plan: "05"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - sites/7greens/sites/7greens/app/page.tsx
  - sites/7greens/sites/7greens/components/chrome/site-header.tsx
  - sites/7greens/sites/7greens/components/chrome/site-footer.tsx
autonomous: false
requirements:
  - FOUND-05

must_haves:
  truths:
    - "At 360px viewport: header logo and mobile menu toggle visible; nav links hidden (desktop nav hidden at <md)"
    - "At 768px viewport: layout transitions cleanly from mobile to desktop breakpoint"
    - "At 1280px viewport: full desktop nav visible; mobile toggle hidden"
    - "No horizontal scroll at any of the three target widths"
    - "Footer grid collapses from 4-column (lg) to 2-column (sm) to 1-column (base) without overflow"
    - "Stub home page renders without layout shift at all three widths"
    - "`pnpm --filter 7greens build` still exits 0"
  artifacts:
    - path: "sites/7greens/sites/7greens/app/page.tsx"
      provides: "Stub home page with SiteSection cid for VCS registration"
      contains: "cid="
    - path: "sites/7greens/sites/7greens/components/chrome/site-header.tsx"
      provides: "Responsive header with md: breakpoint toggle"
      contains: "md:hidden"
  key_links:
    - from: "site-header.tsx mobile nav"
      to: "md breakpoint"
      via: "hidden md:flex (desktop nav) + md:hidden (mobile toggle)"
      pattern: "md:hidden"
    - from: "site-footer.tsx grid"
      to: "responsive columns"
      via: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"
      pattern: "sm:grid-cols"
---

<objective>
Verify and if needed fix the layout at 360px / 768px / 1280px breakpoints. Add a SiteSection cid to the stub home page. Run a human visual check at all three widths.

Purpose: Phase gate — confirms FOUND-05 before moving to Phase 02 brand work.
Output: Confirmed visual layout at all three widths; stub page has VCS cid; any responsive fixes applied.
</objective>

<execution_context>
@C:/Users/benja/Documents/custom_portfolio/.claude/get-anything-done/workflows/execute-plan.md
@C:/Users/benja/Documents/custom_portfolio/.claude/get-anything-done/templates/summary.md
</execution_context>

<context>
@C:/Users/benja/Documents/custom_portfolio/sites/7greens/.planning/ROADMAP.md
@C:/Users/benja/Documents/custom_portfolio/sites/7greens/.planning/phases/01-foundation/01-CONTEXT.md
@C:/Users/benja/Documents/custom_portfolio/sites/7greens/.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Mobile breakpoint targets from RESEARCH.md -->

Tailwind v4 default breakpoints (no custom breakpoints needed):
- (base): 0px — covers 360px mobile-first
- sm: 640px
- md: 768px  ← tablet target; nav toggle point
- lg: 1024px
- xl: 1280px ← desktop target

From RESEARCH.md Mobile Breakpoints Decision:
"The 360px target is covered by the base (0px) styles — no custom breakpoint needed.
Adding xs:360px creates a breakpoint at 360 but base still starts at 0, so it doesn't help.
Mobile-first base styles handle 360+ cleanly."

From chrome components (Plans 03-04):
- SiteHeader: `hidden md:flex` on desktop nav, `md:hidden` on mobile toggle
  → 360px: mobile toggle visible, desktop nav hidden ✓
  → 768px: transition point
  → 1280px: desktop nav visible, mobile toggle hidden ✓
- SiteFooter: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`
  → 360px: single column ✓
  → 768px: two columns ✓
  → 1280px: four columns ✓

From CONTEXT.md:
"No max-width on <main> or <body> — each section clamps itself."
So horizontal scroll risk is from content overflow, not layout container.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stub home page with SiteSection cid + responsive spot-checks</name>
  <files>
    sites/7greens/sites/7greens/app/page.tsx
  </files>

  <read_first>
    - C:/Users/benja/Documents/custom_portfolio/sites/7greens/sites/7greens/app/page.tsx (current file from Plan 01 — adding SiteSection)
    - C:/Users/benja/Documents/custom_portfolio/sites/7greens/sites/7greens/components/chrome/site-header.tsx (check md:hidden classes are present)
    - C:/Users/benja/Documents/custom_portfolio/sites/7greens/sites/7greens/components/chrome/site-footer.tsx (check grid-cols responsive classes are present)
  </read_first>

  <action>
**Step 1: Verify chrome breakpoint classes exist**

Open `components/chrome/site-header.tsx` and confirm:
- Desktop nav has `hidden ... md:flex` (hidden on mobile, shown at md+)
- Mobile toggle button has `md:hidden` (shown on mobile, hidden at md+)

If these classes are missing, add them. The correct pattern from Plan 03 action:
```tsx
// Desktop nav — hidden on mobile:
<nav className="hidden items-center gap-6 md:flex" ...>

// Mobile toggle — hidden on desktop:
<button className="flex items-center justify-center rounded-md p-2 text-muted-foreground hover:text-foreground md:hidden transition-colors" ...>
```

Open `components/chrome/site-footer.tsx` and confirm the grid has:
```tsx
<div className="grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-4">
```
If missing `sm:grid-cols-2` or `lg:grid-cols-4`, add them.

**Step 2: Update app/page.tsx with SiteSection cid**

Replace `sites/7greens/sites/7greens/app/page.tsx` with:

```tsx
import { SiteSection } from "@portfolio/visual-context";

export default function HomePage() {
  return (
    <SiteSection cid="home-stub" id="home">
      <section className="flex min-h-[60vh] flex-col items-center justify-center gap-4 px-4 text-center">
        <h1 className="text-4xl font-bold tracking-tight text-foreground">
          7Greens — Coming Soon
        </h1>
        <p className="max-w-prose text-muted-foreground">
          Chemical-free vegetables from Tyler, TX.{" "}
          <span className="text-sage-text font-medium">
            No membership. No commitment.
          </span>{" "}
          Phase 02 fills this page with the full brand story.
        </p>
      </section>
    </SiteSection>
  );
}
```

Note: `text-sage-text` uses the `--sage-text` token from globals.css (safe contrast variant from RESEARCH.md Pitfall 3).

**Step 3: Run build to confirm no regressions**
```bash
pnpm --filter 7greens build
```
  </action>

  <verify>
    <automated>cd "C:/Users/benja/Documents/custom_portfolio" && pnpm --filter 7greens build 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep 'cid="home-stub"' sites/7greens/sites/7greens/app/page.tsx` exits 0
    - `grep 'md:hidden' sites/7greens/sites/7greens/components/chrome/site-header.tsx` exits 0
    - `grep 'hidden.*md:flex\|md:flex' sites/7greens/sites/7greens/components/chrome/site-header.tsx` exits 0
    - `grep 'sm:grid-cols-2' sites/7greens/sites/7greens/components/chrome/site-footer.tsx` exits 0
    - `grep 'lg:grid-cols-4' sites/7greens/sites/7greens/components/chrome/site-footer.tsx` exits 0
    - `pnpm --filter 7greens build` exits 0
  </acceptance_criteria>

  <done>Stub home page has SiteSection cid="home-stub"; chrome breakpoint classes confirmed; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual breakpoint verification at 360 / 768 / 1280px</name>

  <what-built>
    Full Phase 01 scaffold: Next.js 16 + Tailwind v4 + shadcn init + UPAEC theme tokens + sticky header + footer + VCS provider. Build passes. Dev server runs.
  </what-built>

  <how-to-verify>
    1. Start the dev server:
       ```bash
       pnpm --filter 7greens dev
       ```
       Confirm no startup errors. Open http://localhost:3000.

    2. **360px check** (small phone):
       - Open DevTools → Responsive mode → set width to 360px
       - Expected: Logo visible top-left, hamburger menu icon visible top-right
       - Expected: No desktop nav links visible
       - Expected: No horizontal scroll (page fits at 360px)
       - Click hamburger: mobile nav drawer appears with 4 links
       - Click X or a link: drawer closes

    3. **768px check** (tablet):
       - Set width to 768px
       - Expected: Desktop nav links appear ("Our Story", "Boxes", "Wholesale", "Contact")
       - Expected: Hamburger icon is hidden
       - Expected: Footer shows 2-column grid (nav + contact side by side)

    4. **1280px check** (desktop):
       - Set width to 1280px
       - Expected: Full desktop nav visible, hamburger hidden
       - Expected: Footer shows 4-column grid (brand / empty / nav / contact)
       - Expected: Content is centered with max-w-7xl padding on sides

    5. **VCS dev panel check**:
       - In dev mode (already open), press Alt+I (or check DevIdProvider source for the keybind)
       - Expected: A dev overlay/panel opens showing registered cids
       - Expected: Panel shows at minimum: chrome-header, chrome-footer, home-stub
       - If the keybind doesn't work: open the browser console and type `window.__vcs` or look for a VCS dev badge in the corner

    6. **Theme check**:
       - Background should be off-white cream (#FAF7F0), not pure white or grey
       - Footer background should be slightly different from page body (card: #F4F0E6)
       - Nav links should be muted-foreground color (warm grey/brown), not stark black
  </how-to-verify>

  <resume-signal>
    Type "approved" if all checks pass.
    Type "issues: [description]" if anything is wrong (e.g., "issues: horizontal scroll at 360px", "issues: VCS panel doesn't open").
    Any issues will trigger targeted fixes before the phase is closed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server → browser | Local dev only; no prod data; acceptable for visual verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | 360px overflow causing horizontal scroll | mitigate | Acceptance criteria checks for md:hidden and grid-cols responsive classes; checkpoint verifies visually |
| T-05-02 | Information Disclosure | VCS dev panel in production | accept | DevIdProvider gates on NODE_ENV=development; panel will not render in production build |
</threat_model>

<verification>
Automated:
```bash
cd C:/Users/benja/Documents/custom_portfolio

# Breakpoint classes
grep 'md:hidden' sites/7greens/sites/7greens/components/chrome/site-header.tsx
grep 'sm:grid-cols-2' sites/7greens/sites/7greens/components/chrome/site-footer.tsx

# Build gate
pnpm --filter 7greens typecheck && pnpm --filter 7greens lint && pnpm --filter 7greens build

# VCS cid presence
grep 'cid=' sites/7greens/sites/7greens/app/page.tsx
grep 'cid=' sites/7greens/sites/7greens/components/chrome/site-header.tsx
grep 'cid=' sites/7greens/sites/7greens/components/chrome/site-footer.tsx
```

Manual: see checkpoint task above.
</verification>

<success_criteria>
ROADMAP.md Phase 1 success criteria:
1. `pnpm --filter 7greens dev` boots with no errors — VERIFIED (dev started in checkpoint)
2. `pnpm --filter 7greens build` exits 0 — VERIFIED (automated)
3. shadcn init completed; theme tokens applied — VERIFIED (Plan 01 + 02)
4. Layout (header + footer + main slot) renders on `/` with placeholder content — VERIFIED (checkpoint visual check)
5. VCS provider mounted; dev keybind opens cid inspector showing layout + main-slot cids — VERIFIED (checkpoint VCS check)
All 5 criteria met = Phase 01 DONE.
</success_criteria>

<output>
After completion (checkpoint approved), create `.planning/phases/01-foundation/01-05-SUMMARY.md` with:
- Checkpoint outcome (approved / issues found + fixes applied)
- Visual observations at each breakpoint (pass/fail + notes)
- VCS dev panel observation (cids seen)
- Phase 01 DONE declaration
- Recommended next step: `/gad:execute-phase 02` (Brand / Home page)
</output>
