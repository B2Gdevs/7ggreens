---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - sites/7greens/package.json
  - sites/7greens/tsconfig.json
  - sites/7greens/next.config.ts
  - sites/7greens/postcss.config.mjs
  - sites/7greens/app/globals.css
  - sites/7greens/app/layout.tsx
  - sites/7greens/app/page.tsx
  - sites/7greens/lib/cn.ts
  - sites/7greens/lib/cart-store.ts
  - sites/7greens/README.md
  - sites/7greens/.eslintrc.json
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "`pnpm --filter 7greens build` exits 0 with no TypeScript or lint errors"
    - "The Next.js app at sites/7greens/ has package.json with name `7greens` and all required deps"
    - "No `tailwind.config.ts` file exists — Tailwind v4 is config-free"
    - "postcss.config.mjs contains only `@tailwindcss/postcss` — no autoprefixer"
    - "`next.config.ts` sets `experimental.externalDir: true` and `outputFileTracingRoot` to monorepo root"
    - "`lib/cn.ts` exports a `cn()` helper using clsx + tailwind-merge"
    - "`lib/cart-store.ts` exports `useCartCount(): number` returning 0 (Phase 04 stub seam)"
    - "app/layout.tsx is a Server Component (no `\"use client\"` directive)"
    - "shadcn init completed — `components/ui/` directory exists with at least one primitive"
  artifacts:
    - path: "sites/7greens/package.json"
      provides: "Workspace package manifest with name=7greens and all locked deps"
      contains: '"name": "7greens"'
    - path: "sites/7greens/next.config.ts"
      provides: "Next.js config with externalDir + outputFileTracingRoot"
      contains: "externalDir: true"
    - path: "sites/7greens/postcss.config.mjs"
      provides: "PostCSS config (v4 — no autoprefixer)"
      contains: "@tailwindcss/postcss"
    - path: "sites/7greens/lib/cn.ts"
      provides: "cn() class-name helper"
      exports: ["cn"]
    - path: "sites/7greens/lib/cart-store.ts"
      provides: "Cart count stub seam for Phase 04"
      exports: ["useCartCount"]
    - path: "sites/7greens/app/layout.tsx"
      provides: "Root Server Component layout shell (providers wired in Plan 04)"
      min_lines: 15
  key_links:
    - from: "sites/7greens/next.config.ts"
      to: "packages/visual-context"
      via: "outputFileTracingRoot + externalDir:true"
      pattern: "externalDir.*true"
    - from: "sites/7greens/app/layout.tsx"
      to: "sites/7greens/app/globals.css"
      via: "import"
      pattern: "import.*globals.css"
---

<objective>
Bootstrap the 7greens Next.js application from zero: create the package manifest, install all dependencies, configure Next.js + PostCSS for Tailwind v4, run shadcn init, and create the bare layout shell with supporting utilities.

Purpose: Provides the runnable base every subsequent plan (theme, chrome, VCS) builds into.
Output: A buildable Next.js 16 app at `sites/7greens/` with zero build errors, correct workspace wiring, and stub seams for later phases.
</objective>

<execution_context>
@C:/Users/benja/Documents/custom_portfolio/.claude/get-anything-done/workflows/execute-plan.md
@C:/Users/benja/Documents/custom_portfolio/.claude/get-anything-done/templates/summary.md
</execution_context>

<context>
@C:/Users/benja/Documents/custom_portfolio/sites/7greens/.planning/PROJECT.md
@C:/Users/benja/Documents/custom_portfolio/sites/7greens/.planning/ROADMAP.md
@C:/Users/benja/Documents/custom_portfolio/sites/7greens/.planning/phases/01-foundation/01-CONTEXT.md
@C:/Users/benja/Documents/custom_portfolio/sites/7greens/.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key exports the executor needs from sibling packages. -->

From packages/visual-context/package.json:
- name: "gad-visual-context"
- main: "./dist/index.js"
- type: "module"  ← ESM-only; requires externalDir:true in next.config

From apps/platform/next.config.mjs (canonical pattern — VERIFIED):
```ts
import path from "node:path";
import { fileURLToPath } from "node:url";
const __dirname = path.dirname(fileURLToPath(import.meta.url));
const repoRoot = path.resolve(__dirname, "..", "..");  // two levels up from sites/7greens/
const nextConfig = {
  reactStrictMode: process.env.NODE_ENV === "production",
  outputFileTracingRoot: repoRoot,
  experimental: { externalDir: true },
};
export default nextConfig;
```
NOTE: sites/7greens/ is THREE levels below monorepo root
(monorepo-root / sites / 7greens / sites / 7greens).
So repoRoot = path.resolve(__dirname, "..", "..", "..", "..");  ← four ".." from the Next.js app directory.

From apps/platform/package.json (dep versions to mirror — VERIFIED):
- next: "^16.2.3"  → use "^16.2.4"
- react: "19.0.0"
- react-dom: "19.0.0"
- clsx: "^2.1.1"
- lucide-react: "^1.8.0"
- tailwind-merge: "^2.5.4"
- @tailwindcss/postcss: "^4.0.0"  → use "^4.2.4"
- typescript: "^5"
- tailwindcss: "^4.0.0"  → use "^4.2.4"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write package.json + config files</name>
  <files>
    sites/7greens/package.json
    sites/7greens/tsconfig.json
    sites/7greens/next.config.ts
    sites/7greens/postcss.config.mjs
    sites/7greens/.eslintrc.json
  </files>

  <read_first>
    - C:/Users/benja/Documents/custom_portfolio/apps/platform/next.config.mjs (canonical next config pattern)
    - C:/Users/benja/Documents/custom_portfolio/apps/platform/package.json (dep versions to mirror)
    - C:/Users/benja/Documents/custom_portfolio/packages/visual-context/package.json (ESM package details)
    - C:/Users/benja/Documents/custom_portfolio/sites/7greens/.planning/phases/01-foundation/01-RESEARCH.md (workspace wiring section)
  </read_first>

  <action>
Create the directory `sites/7greens/` and write the following files exactly:

**`sites/7greens/package.json`** — full contents:
```json
{
  "name": "7greens",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbo",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^16.2.4",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "gad-visual-context": "workspace:*",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.4",
    "lucide-react": "^1.8.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^24",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tailwindcss": "^4.2.4",
    "@tailwindcss/postcss": "^4.2.4",
    "postcss": "latest",
    "eslint": "^9",
    "eslint-config-next": "^16.2.4"
  }
}
```

**`sites/7greens/tsconfig.json`** — full contents:
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**`sites/7greens/next.config.ts`** — full contents:
```ts
import path from "node:path";
import { fileURLToPath } from "node:url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
// monorepo root is FOUR levels up: sites/7greens → sites/7greens/sites → sites/7greens → sites → (root)
const repoRoot = path.resolve(__dirname, "..", "..", "..", "..");

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: process.env.NODE_ENV === "production",
  // Allows Next.js file tracer to follow pnpm workspace symlinks
  // from sites/7greens/ up to packages/visual-context/.
  // DO NOT use transpilePackages — externalDir is the correct pattern (verified from apps/platform).
  outputFileTracingRoot: repoRoot,
  experimental: {
    externalDir: true,
  },
};

export default nextConfig;
```

**`sites/7greens/postcss.config.mjs`** — full contents:
```js
/** @type {import('postcss').Config} */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
```
CRITICAL: Do NOT add `autoprefixer` here. Tailwind v4 does not need it and it produces build warnings. [VERIFIED: apps/platform/postcss.config.mjs]

**`sites/7greens/.eslintrc.json`** — full contents:
```json
{
  "extends": ["next/core-web-vitals", "next/typescript"]
}
```

Do NOT create a `tailwind.config.ts` — Tailwind v4 is CSS-only; all theme tokens go in `globals.css` via `@theme inline`. [VERIFIED: RESEARCH.md Pitfall 2]
  </action>

  <verify>
    <automated>
      cd "C:/Users/benja/Documents/custom_portfolio/sites/7greens" && cat package.json | grep '"name"' | grep '7greens'
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep '"name": "7greens"' sites/7greens/package.json` exits 0
    - `grep '"externalDir": true' sites/7greens/next.config.ts` exits 0 OR `grep 'externalDir: true' sites/7greens/next.config.ts` exits 0
    - `grep 'outputFileTracingRoot' sites/7greens/next.config.ts` exits 0
    - `grep '@tailwindcss/postcss' sites/7greens/postcss.config.mjs` exits 0
    - `ls sites/7greens/tailwind.config.ts 2>/dev/null` returns nothing (file MUST NOT exist)
    - `grep 'autoprefixer' sites/7greens/postcss.config.mjs 2>/dev/null` exits non-zero (no autoprefixer)
    - `grep '"gad-visual-context": "workspace:\*"' sites/7greens/package.json` exits 0
  </acceptance_criteria>

  <done>All config files written; package.json references gad-visual-context as workspace:*; no tailwind.config.ts; postcss.config.mjs has @tailwindcss/postcss only.</done>
</task>

<task type="auto">
  <name>Task 2: Install deps + init shadcn + create app skeleton</name>
  <files>
    sites/7greens/app/globals.css
    sites/7greens/app/layout.tsx
    sites/7greens/app/page.tsx
    sites/7greens/lib/cn.ts
    sites/7greens/lib/cart-store.ts
    sites/7greens/README.md
    sites/7greens/components/ui/  (populated by shadcn)
  </files>

  <read_first>
    - C:/Users/benja/Documents/custom_portfolio/sites/7greens/.planning/phases/01-foundation/01-RESEARCH.md (Pitfall 1: VCS dist must exist; Pitfall 2: tailwind.config.ts must not exist)
    - C:/Users/benja/Documents/custom_portfolio/apps/platform/app/layout.tsx (Server Component layout pattern)
  </read_first>

  <action>
Run these commands from the monorepo root (C:/Users/benja/Documents/custom_portfolio):

```bash
# Step 1: Ensure gad-visual-context dist is built (RESEARCH.md Pitfall 1)
pnpm --filter gad-visual-context build

# Step 2: Install 7greens deps (pnpm workspace auto-links gad-visual-context)
pnpm --filter 7greens install

# Step 3: Init shadcn — New York style, Tailwind v4, yes to all prompts
# Run from INSIDE the app directory (shadcn reads local package.json)
cd sites/7greens && pnpm dlx shadcn@latest init --yes
```

After shadcn init, it may create or overwrite:
- `app/globals.css` — shadcn writes its own CSS variables block. We will REPLACE this in Plan 02 with the UPAEC palette. For now, accept whatever shadcn generates (it proves Tailwind v4 is working).
- `lib/utils.ts` — shadcn's default cn() location. Accept it.
- `components/ui/` — populated with primitives as selected.

If shadcn init is non-interactive (--yes flag doesn't work), run it interactively with these selections:
- Style: New York
- Base color: Zinc (closest to our charcoal; palette overridden in Plan 02)
- CSS variables: Yes
- React Server Components: Yes
- Tailwind v4: Yes (if prompted)

After shadcn init completes, write these files (overwrite if shadcn created them):

**`sites/7greens/lib/cn.ts`** — write exactly (even if shadcn created lib/utils.ts; both can coexist):
```ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**`sites/7greens/lib/cart-store.ts`** — Phase 04 stub seam:
```ts
/**
 * Cart store stub — Phase 04 replaces the body with Zustand/Context
 * reading Square Order state. The hook shape (useCartCount → number)
 * must not change so SiteHeader's import remains stable.
 */
export function useCartCount(): number {
  // Phase 04: replace with actual cart state
  return 0;
}
```

**`sites/7greens/app/layout.tsx`** — bare Server Component shell (VCS provider + chrome wired in Plans 03 and 04):
```tsx
import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";

const geist = Geist({
  subsets: ["latin"],
  variable: "--font-geist",
});

export const metadata: Metadata = {
  title: "7Greens | Uncle Paul's Agritourism & Educational Corp",
  description:
    "Chemical-free, non-GMO produce from Tyler, TX. Order a flexible vegetable box — no membership, no commitment.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={geist.variable}>
      <body className="bg-background text-foreground antialiased">
        {/* SiteVisualContextProvider, SiteHeader, SiteFooter added in Plans 03 + 04 */}
        <main className="flex-1">{children}</main>
      </body>
    </html>
  );
}
```

**`sites/7greens/app/page.tsx`** — stub home:
```tsx
export default function HomePage() {
  return (
    <section className="flex min-h-[60vh] flex-col items-center justify-center gap-4 px-4 text-center">
      <h1 className="text-4xl font-bold tracking-tight">
        7Greens — Coming Soon
      </h1>
      <p className="max-w-prose text-muted-foreground">
        Chemical-free vegetables from Tyler, TX. Phase 02 fills this page.
      </p>
    </section>
  );
}
```

**`sites/7greens/README.md`** — minimal:
```md
# 7Greens (UPAEC)

Next.js 16 App Router site for Uncle Paul's Agritourism & Educational Corp.

## Dev

```bash
# From monorepo root
pnpm --filter 7greens dev      # starts on :3000 (turbo)
pnpm --filter 7greens build    # production build check
pnpm --filter 7greens typecheck
```

## Prerequisites

`gad-visual-context` dist must be built before first run:
```bash
pnpm --filter gad-visual-context build
```
```

Finally, verify the build compiles:
```bash
# From monorepo root
pnpm --filter 7greens build
```
If build fails with "Cannot find module 'gad-visual-context'", run:
```bash
pnpm --filter gad-visual-context build
pnpm --filter 7greens build
```
  </action>

  <verify>
    <automated>cd "C:/Users/benja/Documents/custom_portfolio" && pnpm --filter 7greens build 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter 7greens build` exits 0 (zero TypeScript errors, zero lint errors)
    - `ls sites/7greens/components/ui/` shows at least one `.tsx` file (shadcn init ran)
    - `grep 'useCartCount' sites/7greens/lib/cart-store.ts` exits 0
    - `grep 'Geist' sites/7greens/app/layout.tsx` exits 0
    - `grep '"use client"' sites/7greens/app/layout.tsx 2>/dev/null` exits non-zero (layout MUST be a Server Component)
    - `ls sites/7greens/tailwind.config.ts 2>/dev/null` returns nothing (must not exist)
  </acceptance_criteria>

  <done>`pnpm --filter 7greens build` passes; shadcn init complete; lib/cn.ts and lib/cart-store.ts exist; layout.tsx is a Server Component; no tailwind.config.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace symlink → package dist | pnpm symlinks from sites/7greens/node_modules to packages/visual-context/dist — Next.js tracer must follow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | postcss.config.mjs | accept | Scaffold-only phase; no user input; file is read-only config |
| T-01-02 | Information Disclosure | next.config.ts outputFileTracingRoot | accept | Traces to monorepo root; no secrets in file tree at this stage |
| T-01-03 | Denial of Service | shadcn init failing silently | mitigate | Acceptance criteria requires `ls components/ui/` shows files — catches silent failure |
</threat_model>

<verification>
From monorepo root:

```bash
pnpm --filter 7greens typecheck && pnpm --filter 7greens lint && pnpm --filter 7greens build
```

All three must exit 0.

Spot checks:
- `grep '"name": "7greens"' sites/7greens/package.json`
- `grep 'externalDir: true' sites/7greens/next.config.ts`
- `ls sites/7greens/tailwind.config.ts 2>/dev/null` → empty (no file)
- `grep 'autoprefixer' sites/7greens/postcss.config.mjs 2>/dev/null` → empty (no autoprefixer)
</verification>

<success_criteria>
- `pnpm --filter 7greens build` exits 0 with no errors
- `sites/7greens/` directory contains: package.json, tsconfig.json, next.config.ts, postcss.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, lib/cn.ts, lib/cart-store.ts, components/ui/
- No tailwind.config.ts exists
- next.config.ts has `externalDir: true` and `outputFileTracingRoot`
- postcss.config.mjs has `@tailwindcss/postcss` only (no autoprefixer)
- `gad-visual-context` is listed as `workspace:*` in package.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- Files created (exact paths)
- shadcn version that ran (from `pnpm dlx shadcn@latest --version` output)
- Any deviations from the plan (e.g., if shadcn created lib/utils.ts instead of lib/cn.ts)
- Build output confirmation (exit code, any warnings)
- Exact repoRoot path computed in next.config.ts (four levels up from app dir)
</output>
